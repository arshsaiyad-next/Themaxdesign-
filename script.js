// script.js
// // // document.addEventListener('DOMContentLoaded', () => {
    // Smooth scrolling for navbar links
                    const navLinks = document.querySelectorAll('.nav-links a');
                                                            navLinks.forEach(link => {
                                                                                                                                                link.addEventListener('click', (e) => {                       const observer = new IntersectionObserver((entries) =>                       const observer = new IntersectionObserver((entries) =>                       const observer = new IntersectionObserver((entries) =>   const observer = new IntersectionObserver((entries) =>   const observer = new IntersectionObserver((entries) =>